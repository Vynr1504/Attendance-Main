export class SecFacultyMap {
  constructor(batch, department, section, course, map) {
    (this.batch = batch),
      (this.department = department),
      (this.section = section),
      (this.course = course),
      (this.map = map);
  }
}
